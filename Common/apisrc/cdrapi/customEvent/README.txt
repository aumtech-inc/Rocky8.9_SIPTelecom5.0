
The customEvent binary was requested by the Pune team to log a customEvent record from a java program.  
So it's a stand-a-lone binary which takes a single text string (in quotes) as an argument.

	[skywalker] ./customEvent 
	Usage: ./customEvent <string in quotes>

